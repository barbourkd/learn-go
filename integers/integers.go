package integers

// Add takes two integers and returns the sum of them
func Add(x, y int) int {
	return x + y
}

// Subtract takes two integers and returns the difference of x and y
func Subtract(x, y int) int {
	return x - y
}
